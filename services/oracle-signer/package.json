{
  "name": "yap-oracle-signer",
  "version": "0.1.0",
  "private": true,
  "description": "Yap TEE-attested verdict oracle signer. Designed to deploy to Phala dstack; runs locally via simulator or fallback for development.",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@phala/dstack-sdk": "^0.5.7",
    "ethers": "^6.16.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
