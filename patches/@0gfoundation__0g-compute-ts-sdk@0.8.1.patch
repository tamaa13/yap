diff --git a/lib.commonjs/broker.js b/lib.commonjs/broker.js
index cfa5f2cf26c57f8ad1a96a644290d4e57c54c553..5866740939cdfc2f2f6d8e08b7e7acfdd265d4d0 100644
--- a/lib.commonjs/broker.js
+++ b/lib.commonjs/broker.js
@@ -84,7 +84,7 @@ async function createZGComputeNetworkBroker(signer, ledgerCA, inferenceCA, fineT
         const ledger = await (0, ledger_1.createLedgerBroker)(signer, finalLedgerCA, finalInferenceCA, finalFineTuningCA, gasPrice, maxGasPrice, step);
         const inferenceBroker = await (0, broker_2.createInferenceBroker)(signer, finalInferenceCA, ledger);
         let fineTuningBroker;
-        if (signer instanceof ethers_1.Wallet) {
+        if (signer && typeof signer.signTransaction === "function") {
             fineTuningBroker = await (0, broker_1.createFineTuningBroker)(signer, finalFineTuningCA, ledger, gasPrice, maxGasPrice, step);
         }
         const broker = new ZGComputeNetworkBroker(ledger, inferenceBroker, fineTuningBroker);
diff --git a/lib.esm/index-a83ec133.js b/lib.esm/index-a83ec133.js
index f19d84f9af350c834b9d084eb4680f2f76f4b5a4..1ea4ba6afa4607a486ab85616425df8ac5ef7fb3 100644
--- a/lib.esm/index-a83ec133.js
+++ b/lib.esm/index-a83ec133.js
@@ -23142,7 +23142,7 @@ class LedgerBroker {
         const ledgerContract = new LedgerManagerContract(this.signer, this.ledgerCA, userAddress, this.gasPrice, this.maxGasPrice, this.step);
         const inferenceContract = new InferenceServingContract(this.signer, this.inferenceCA, userAddress);
         let fineTuningContract;
-        if (this.signer instanceof Wallet) {
+        if (this.signer && typeof this.signer.signTransaction === "function") {
             fineTuningContract = new FineTuningServingContract(this.signer, this.fineTuningCA, userAddress);
         }
         // Get service names from contract using getServiceInfo
@@ -23158,7 +23158,7 @@ class LedgerBroker {
             const inferenceServiceName = inferenceServiceInfo.fullName;
             // Get service info for fine-tuning contract if using Wallet
             let fineTuningServiceName;
-            if (this.signer instanceof Wallet) {
+            if (this.signer && typeof this.signer.signTransaction === "function") {
                 try {
                     const fineTuningServiceInfo = await ledgerContract.getServiceInfo(fineTuningCA);
                     fineTuningServiceName = fineTuningServiceInfo.fullName;
@@ -23619,7 +23619,7 @@ async function createZGComputeNetworkBroker(signer, ledgerCA, inferenceCA, fineT
         const ledger = await createLedgerBroker(signer, finalLedgerCA, finalInferenceCA, finalFineTuningCA, gasPrice, maxGasPrice, step);
         const inferenceBroker = await createInferenceBroker(signer, finalInferenceCA, ledger);
         let fineTuningBroker;
-        if (signer instanceof Wallet) {
+        if (signer && typeof signer.signTransaction === "function") {
             fineTuningBroker = await createFineTuningBroker(signer, finalFineTuningCA, ledger, gasPrice, maxGasPrice, step);
         }
         const broker = new ZGComputeNetworkBroker(ledger, inferenceBroker, fineTuningBroker);
